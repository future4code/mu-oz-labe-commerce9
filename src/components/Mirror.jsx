import React from 'react';

export default function Mirror ({count}) {
    return (
        <span>
            <b>
                Mirror:
            </b>
            {count}
        </span>
    );
}